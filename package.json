{
  "name": "music-agent-harness",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "description": "A sealed developmental harness for one continuing subject.",
  "exports": {
    ".": "./src/index.js",
    "./rehearsal": "./src/rehearsal.js"
  },
  "bin": {
    "music": "./src/cli.js",
    "music-doctor": "./bin/music-doctor.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js bin/*.js && node --test",
    "rehearse": "node src/cli.js rehearse"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^3.0.0",
    "ai": "^7.0.85",
    "zod": "^4.1.8"
  }
}
